# 📦 구조 분해 할당과 스프레드 연산자

배열/객체의 값을 꺼내거나 합칠 때 사용하는 ES6 문법을 정리합니다.

---

## 1️⃣ 구조 분해 할당 (Destructuring)

```js
const user = { name: "재석", age: 30 };
const { name, age: userAge = 20 } = user;
// name = "재석", userAge = 30

const [first, , third] = [1, 2, 3];
// first = 1, third = 3
```

- 객체는 **키 이름**으로, 배열은 **순서(인덱스)**로 매칭
- 기본값(`=`)과 별칭(`:`) 동시 사용 가능
