# 🗜️ Nginx Gzip 압축 설정

텍스트 기반 응답(HTML, CSS, JS, JSON)을 압축해 전송하면 네트워크 전송량과 로딩 시간을 크게 줄일 수 있습니다.

---

## 1️⃣ 핵심 지시어

| 지시어 | 설명 |
|--------|------|
| `gzip on` | Gzip 압축 활성화 |
| `gzip_types` | 압축할 MIME 타입 지정 |
| `gzip_comp_level` | 압축률(1~9), 높을수록 CPU 사용량 증가 |
| `gzip_min_length` | 이보다 작은 응답은 압축하지 않음 |
