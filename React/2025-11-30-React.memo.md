# React.memo
`React.memo`는 **컴포넌트의 불필요한 리렌더링을 방지하는 고차 컴포넌트(HOC, Higher-Order Component)** 로, `props`가 바뀌지 않았다면 기존 렌더링 결과를 재사용하여 성능을 개선합니다.

---

## 1️⃣ React.memo를 사용하는 이유

- 부모가 렌더링될 때 **자식도 매번 리렌더링되는 문제 해결**
- **리렌더링 비용이 큰 컴포넌트 최적화**
- 동일한 UI를 다시 계산하지 않도록 **렌더링 결과 재사용**

---

