# 🚀 React-Redux

React는 UI 렌더링을 담당하고, Redux는 상태관리(state management)를 담당합니다.  
두 라이브러리를 함께 사용할 때는 **React-Redux**라는 공식 바인딩 라이브러리를 사용합니다.

React-Redux는 다음 역할을 합니다.

- Redux store를 React 컴포넌트 트리에 제공
- 상태 변화가 발생하면 필요한 컴포넌트만 리렌더
- dispatch를 손쉽게 사용할 수 있게 도와줌

---