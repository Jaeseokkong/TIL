# ⚖️ 두 개의 힙으로 데이터 스트림의 중앙값 구하기

숫자가 계속 추가되는 상황에서 **매번 정렬하지 않고** 중앙값을 빠르게 구하는 문제입니다.

---

## 1️⃣ 문제 접근

- **Max-Heap**에는 작은 절반, **Min-Heap**에는 큰 절반을 유지
- 두 힙의 크기 차이를 최대 1로 유지하도록 균형을 맞춤
- 중앙값은 두 힙의 루트(top)만 보면 O(1)에 구할 수 있음
