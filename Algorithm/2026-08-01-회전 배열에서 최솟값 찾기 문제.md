# 🔄 회전된 정렬 배열에서 최솟값 찾기

오름차순으로 정렬된 배열을 임의의 지점에서 회전시켰을 때, 최솟값(회전 지점)을 O(log n)에 찾는 문제입니다.

---

## 1️⃣ 문제 접근

- 이진 탐색을 응용: `mid` 값과 `right` 값을 비교
- `nums[mid] > nums[right]` → 최솟값은 mid보다 오른쪽에 있음
- `nums[mid] <= nums[right]` → 최솟값은 mid를 포함해 왼쪽에 있음
