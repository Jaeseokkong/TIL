# 🌳 트라이(Trie)로 문자열 검색하기

여러 단어의 **공통 접두사(prefix)를 트리 구조로 공유**해, 문자열 검색과 자동완성을 빠르게 처리하는 자료구조입니다.

---

## 1️⃣ 핵심 개념

- 각 노드는 알파벳 하나를 나타내고, 자식 노드로 다음 글자를 연결
- 단어의 끝 지점에는 `isEnd` 표시를 둠
- 검색/삽입 모두 **단어 길이에 비례한 O(L)** 시간에 처리 가능
