# 🪟 슬라이딩 윈도우로 부분합 구하기

크기가 고정된 부분 배열의 합/최댓값 등을 구할 때, 매번 처음부터 다시 계산하지 않고
**윈도우를 한 칸씩 이동시키며 누적값만 갱신**하는 기법입니다.

---

## 1️⃣ 문제 접근

- 크기 k인 윈도우의 합을 먼저 구함
- 윈도우를 오른쪽으로 한 칸 옮길 때 `+새로 들어온 값 - 빠져나간 값`으로 O(1) 갱신
- 전체 O(n²) → O(n)으로 개선
