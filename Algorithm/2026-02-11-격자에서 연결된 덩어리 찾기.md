# 🥖 2차원 격자에서 연결된 덩어리 착기

2차얼 격자 문제에서 상하좌우로 연결된 영역(덩어리)의 문제들은 섬 개수, 영역 크기 계산, 퍼짐 시뮬레이션 등 다양한 문제에서 반복적으로 등장합니다.

핵심은 **BFS 또는 DFS를 이용하여 연결된 영역을 한 번에 탐색하는 것**입니다.

---

## 1️⃣ 핵심 개념

### 🔹 연결된 덩어리란?

- 상, 하, 좌, 우로 이어진 동일한 값의 집합
- 하나의 시작점에서 BFS/DFS를 수행하면 하나의 덩어리를 전부 방문 가능

---

### 🔹 기본 문제 패턴

1. 격자를 전체 순회
2. 아직 방문하지 않은 특정 값(예: 1)을 발견
3. BFS/DFS 시작
4. 연결된 영역 전부 방문 처리
5. BFS/DFS 1회 = 덩어리 1개

---

### 🔹 거의 등장하는 요소

- 방문 배열 (visited)
- 방향 배열 (dx, dy)
- 범위 체크 (nx >= 0 ...)

---