# 🏗️ Spring 계층 구조 정리 

## 1️⃣ 구조 개요
```markdown
Controller → Service → DAO/Repository → Entity
       ↕
     DTO (입출력용)
```

|계층|역할|
|Controller|클라이언트 요청 처리, DTO 변환, 결과 반환|
|Service|비지니스 로직, 검증, Entity 생성/업데이트|
|DAO / Respository|DB 접근, Entity CRUD 수행|
|Entity|DB 테이블과 매핑되는 객체, 영속성 관리|
|DTO|데이터 전송 객체, API 요청/응답 용도|

---

## 2️⃣ Entity (Reservation)

- DB 테이블과 1:1 매핑되는 객체
- JPA 어노테이션으로 필드 속성, PK, 생성/수정일 자동 처리

```java
@Entity
@Table(name = "reservation")
public class Reservation {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(nullable = false, unique = true, length = 64)
    private String token;

    @Column(name = "customer_name", nullable = false, length = 200)
    private String customerName;

    @Enumerated(EnumType.STRING)
    private Status status;

    @Temporal(TemporalType.TIMESTAMP)
    private Date createdAt;

    @Temporal(TemporalType.TIMESTAMP)
    private Date updatedAt;

    @PrePersist
    protected void onCreate() {
        createdAt = new Date();
        updatedAt = new Date();
        status = Status.ACTIVE;
    }

    @PreUpdate
    protected void onUpdate() {
        updatedAt = new Date();
    }

    // getter / setter ...
}
```
### 🔹 포인트
- Entity는 **DB 중심 설계**
- 상태값, 날짜는 **Enum, Temporal**로 처리
- @PrePersist / @PreUpdate로 **생성/수정일 자동 관리**

---