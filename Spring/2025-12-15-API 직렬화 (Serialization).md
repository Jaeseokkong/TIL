# 🖨️ API 직렬화 (Serialization)

API에서 **직렬화(Serialization)** 는 서버 내부에서 사용하는 객체를 **클라이언트와 통신 가능한 데이터 현식(JSON, XML 등)** 으로 변환하는 과정입니다.

---